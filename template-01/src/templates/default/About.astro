---
import type { ClientContent } from "../../schema/content.schema";

interface Props {
	sections: ClientContent["sections"];
}
const { about } = Astro.props.sections;
---

<section class="about">
	<div class="about-inner">
		{
			about.about_image && (
				<img
					src={about.about_image}
					alt="Foto locale"
					class="about-img"
				/>
			)
		}
		<div class="about-text">
			<h2 data-editable="about_preTitle">{about.about_preTitle}</h2>
			<div data-editable="about_text" set:html={about.about_text} />
		</div>
	</div>
</section>
